        apiVersion: redhatcop.redhat.io/v1alpha1
        kind: KubernetesSecretEngineRole
        metadata:
          name: "tenant-gitops"
          namespace: vault-admin
        spec:
          authentication: 
            path: ${CLUSTER_NAME}
            role: vault-admin
          path: /infra/clusters/${CLUSTER_NAME}/jwt
          serviceAccountName: "tenant-gitops"
          allowedKubernetesNamespaces: 
          - vault-admin