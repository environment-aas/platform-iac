apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../components/approved-namespaces-app/

patches:

- patch: |-
    - op: replace
      path: /spec/source/path
      value: "non-prod"
  target:
    kind: Application